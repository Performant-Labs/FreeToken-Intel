[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "freetoken-kernel-cache"
version = "0.0.0"
description = "AOT SYCL/Triton-Intel kernel cache for FreeToken-Intel (Arc Pro B70)"
requires-python = ">=3.10"
license = "Apache-2.0"
